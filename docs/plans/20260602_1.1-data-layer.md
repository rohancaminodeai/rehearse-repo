---
task: 1.1
title: lib/db.ts — customers + records CRUD with scrypt hashing (TDD AC1–AC5)
size: S
status: complete
depends-on: 0.2
pr: (pre-git)
---

# Data layer (TDD)

## Context
Implement the Postgres data layer following TDD: write failing tests first
(using pg-mem, no live DB needed), then implement to green. Advances Phase 1
exit criteria (unit tests green).

## Goals
- `createCustomer`, `listCustomers`, `authenticateCustomer`, `addRecord`,
  `getRecordsByCustomer`, `getCustomer` implemented and tested
- AC1–AC5 from SPEC.md all green

## Key decisions
- pg-mem used for tests (no live Postgres required in CI)
- `setPool()` injection point allows tests to replace the real pool
- Passwords hashed with `scryptSync`; `verifyPassword` is constant-time

## Summary
- **What changed:** `lib/db.ts` (95 LOC), `lib/db.test.ts` (5 tests → later 12)
- **Outcome:** AC1–AC5 green via pg-mem; scrypt hashing verified (hash ≠ plaintext)
