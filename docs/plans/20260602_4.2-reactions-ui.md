---
task: 4.2
title: Customer emoji reaction UI — pills + toggleReaction server action
size: S
status: in-progress
depends-on: 4.1
pr: #7
---

# Customer reaction UI

## Context
Wire `db.toggleReaction` + `db.getReactions` into the customer portal as 5 emoji
pills per record. Customers tap to toggle their reaction. Advances Phase 4.

## Goals
- `toggleReactionAction` server action in `app/actions.ts`
- Customer portal shows 5 emoji pills per record with counts
- Only the owning customer can react (enforced at db layer from 4.1)

## Files touched
| File | Change |
|------|--------|
| `app/actions.ts` | add `toggleReactionAction` |
| `lib/db.ts` | `getRecordsByCustomer` extended to fetch reactions per record |
| `app/page.tsx` | render emoji pills |

## Test plan
- [ ] `npm test` — 29 tests still green
- [ ] Reaction pills render on customer portal

## Rollout
- **Branch:** `feat/4.2-reactions-ui`
- **Migration / env changes:** None
