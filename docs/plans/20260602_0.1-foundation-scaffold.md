---
task: 0.1
title: Next.js 15 + TS + Vitest + Docker + Postgres scaffold
size: S
status: complete
depends-on: —
pr: (pre-git)
---

# Foundation scaffold

## Context
Bootstrap the project: Next.js 15 App Router, TypeScript, Vitest for TDD,
Docker Compose with Postgres 16, and a minimal package.json. Advances Phase 0
exit criteria (app runs via `docker compose up`; tests pass).

## Goals
- Next.js 15 app boots with `npm run dev`
- `docker compose up` starts web + postgres; web is healthy
- `npm test` runs (even with no tests yet)

## Non-Goals
- Business logic, pages, auth — deferred to later tasks

## What was built
- `package.json`, `tsconfig.json`, `next.config.mjs`
- `Dockerfile` (multi-stage: deps → builder → runner), `docker-compose.yml`
- `.env.example`, `.gitignore`, `.dockerignore`
- `app/layout.tsx` (minimal shell)

## Summary
- **What changed:** project scaffolded from scratch; Docker stack verified healthy
- **Outcome:** `docker compose up --build` → web on :3000, Postgres on :5432
